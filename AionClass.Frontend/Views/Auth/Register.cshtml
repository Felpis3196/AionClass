@model AionClass.Frontend.Models.Auth.RegisterRequest

@{
    ViewData["Title"] = "Register";
    ViewData["BodyClass"] = "auth-page-body";
    Layout = "_Layout";
    ViewData["HideFooter"] = true;
}

<div class="auth-container">
    <aside class="auth-sidebar">
        <div class="auth-header-summary">
            <div class="auth-avatar">
                <i class="fas fa-user-circle"></i>
            </div>
            <h2 class="auth-username">Bem-vindo!</h2>
            <p class="auth-welcome-message">Crie sua conta para começar</p>
        </div>

        <nav class="auth-nav">
            <ul>
                <li><a asp-action="Login" asp-controller="Auth">Login</a></li>
                <li><a asp-action="Register" asp-controller="Auth">Register</a></li>
            </ul>
        </nav>
    </aside>

    <main class="auth-content">
        <section class="auth-section active">
            <h3 class="section-title">Registro</h3>
            <p class="section-description">Preencha os campos abaixo para criar sua conta.</p>

            <form asp-action="Register" method="post" class="auth-form">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <div class="form-group">
                    <label asp-for="PrimeiroNome"></label>
                    <input asp-for="PrimeiroNome" class="form-control" />
                    <span asp-validation-for="PrimeiroNome" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Sobrenome"></label>
                    <input asp-for="Sobrenome" class="form-control" />
                    <span asp-validation-for="Sobrenome" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Email"></label>
                    <input asp-for="Email" class="form-control" type="email" />
                    <span asp-validation-for="Email" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Password"></label>
                    <input asp-for="Password" class="form-control" type="password" />
                    <span asp-validation-for="Password" class="text-danger"></span>
                </div>

                <div class="form-group d-none">
                    <label asp-for="PerfilUsuario"></label>
                    <select asp-for="PerfilUsuario" class="form-control">
                        <option value="">Selecione</option>
                        <option value="Student">Estudante</option>
                        <option value="Instructor">Instrutor</option>
                        <option value="Admin">Administrador</option>
                    </select>
                    <span asp-validation-for="PerfilUsuario" class="text-danger"></span>
                </div>

                <div class="form-group d-none">
                    <label asp-for="Role"></label>
                    <input asp-for="Role" class="form-control" />
                    <span asp-validation-for="Role" class="text-danger"></span>
                </div>

                <button type="submit" class="save-button">Registrar</button>
            </form>
        </section>
    </main>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const navItems = document.querySelectorAll('.auth-nav-item');
            const sections = document.querySelectorAll('.auth-section');

            navItems.forEach(item => {
                item.addEventListener('click', function (e) {
                    e.preventDefault();
                    navItems.forEach(nav => nav.classList.remove('active'));
                    this.classList.add('active');
                    sections.forEach(sec => sec.classList.remove('active'));
                    const targetSectionId = this.dataset.section;
                    const targetSection = document.getElementById(targetSectionId);
                    if (targetSection) {
                        targetSection.classList.add('active');
                    }
                });
            });
        });
    </script>
}
